source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins

# Needed for Ruby ≥ 3
gem "webrick", "~> 1.8"

# Keep ONLY if you really need this exact version
gem "connection_pool", "2.5.0"
